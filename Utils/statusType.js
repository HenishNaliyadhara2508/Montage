// Define an object to store the status types
export const statusType = {
    UNPUBLISH: 'unpublish',  // Represents when a substyle is unpublished
    DRAFT: 'draft',          // Represents when a substyle is in draft status
    PUBLISH: 'publish',      // Represents when a substyle is published
  };
  